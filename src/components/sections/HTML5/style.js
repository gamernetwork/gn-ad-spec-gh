import styled from 'styled-components';
import StyledSection from '../StyledSection';

const StyledHTML5Section = styled(StyledSection)`
  p:first-of-type {
    margin-top: 0;
  }
`;

export default StyledHTML5Section;
