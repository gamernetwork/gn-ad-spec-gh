import styled from 'styled-components';

const StyledSection = styled.section`
  position: relative;
  width: 100%;

  h2 {
    position: absolute;
    margin: 0;
  }
`;

export default StyledSection;
